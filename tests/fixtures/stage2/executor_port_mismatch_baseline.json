{
  "_comment": "Nodes whose executor returns a flow port their contract does not declare. Recorded at PHASE 9 recovery item 6 with 45 entries, reduced to 1 by item 7, and to 0 by item 8 -- the last entry was set_variable, whose 'done' return was untangled once its duplicate executor was resolved. The list is deliberately kept in place while empty: the gate in tools/audit_node_system.py fails on any NEW entry, on any entry left here after it is fixed, and on any entry whose ports changed, so an empty file is the strongest possible statement and the check that enforces it is the same one that carried the debt down from 45.",
  "_provenance": "PHASE 9 recovery item 8, regenerated from tools/audit_node_system.py:executor_output_violations(). 0 assets were modified.",
  "count": 0,
  "nodes": {},
  "_reasons": {}
}
