"""Removed shell pass-through layer.

The official editor now wires focused controllers directly.  This module is
kept temporarily as an import-safe migration marker for external tooling; it
contains no runtime adapters and may be removed in v2.0.
"""
