[1mdiff --git a/engine/physics/collider.py b/engine/physics/collider.py[m
[1mindex 6af14fe..8a9bb05 100644[m
[1m--- a/engine/physics/collider.py[m
[1m+++ b/engine/physics/collider.py[m
[36m@@ -1,9 +1,9 @@[m
 from __future__ import annotations[m
[31m-from typing import Optional, Callable, List, Dict, Any, TYPE_CHECKING[m
[32m+[m
[32m+[m[32mfrom typing import TYPE_CHECKING, Any, Callable, Dict, List, Optional[m
 from dataclasses import dataclass, field[m
 import math[m
[31m-import pygame[m
[31m-from engine.component import Component[m
[32m+[m[32mfrom engine.core.component import Component[m
 [m
 if TYPE_CHECKING:[m
     from engine.game_object import GameObject[m
[36m@@ -73,14 +73,22 @@[m [mclass BoxCollider(Component):[m
     # ------------------------------------------------------------------[m
 [m
     @property[m
[31m-    def rect(self) -> pygame.Rect:[m
[31m-        """Retorna o pygame.Rect atual no espaço de mundo."""[m
[32m+[m[32m    def aabb(self) -> tuple[float, float, float, float]:[m
[32m+[m[32m        """Bounding box no espaço mundo: (left, top, right, bottom). Headless — sem pygame."""[m
         if self.game_object is None:[m
[31m-            return pygame.Rect(0, 0, int(self.width), int(self.height))[m
[32m+[m[32m            return (self.offset_x - self.width / 2, self.offset_y - self.height / 2,[m
[32m+[m[32m                    self.offset_x + self.width / 2, self.offset_y + self.height / 2)[m
         pos = self.game_object.transform.get_world_position()[m
[31m-        left = int(pos[0] + self.offset_x - self.width / 2)[m
[31m-        top  = int(pos[1] + self.offset_y - self.height / 2)[m
[31m-        return pygame.Rect(left, top, int(self.width), int(self.height))[m
[32m+[m[32m        left  = pos[0] + self.offset_x - self.width  / 2[m
[32m+[m[32m        top   = pos[1] + self.offset_y - self.height / 2[m
[32m+[m[32m        return (left, top, left + self.width, top + self.height)[m
[32m+[m
[32m+[m[32m    @property[m
[32m+[m[32m    def rect(self):[m
[32m+[m[32m        """Retorna um pygame.Rect no espaço mundo (requer pygame)."""[m
[32m+[m[32m        import pygame as _pg[m
[32m+[m[32m        left, top, right, bottom = self.aabb[m
[32m+[m[32m        return _pg.Rect(int(left), int(top), int(self.width), int(self.height))[m
 [m
     @classmethod[m
     def invalidate_tilemap_cache(cls, scene: Any) -> None:[m
