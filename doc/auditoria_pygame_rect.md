# Relatorio de Auditoria: Construcao do pygame.Rect no Blit

Gerado em: 2026-07-11 13:48:09

## Logs de Metricas de Bounding Box

### Registro 0 (rz: 0.0°)
- **Transform.position**: `(0.000000, 120.000000)`
- **screen_x / screen_y**: `(400.0000, 420.0000)`
- **Surface (antes rotação)**: `(600, 32)`
- **Surface (após rotação)**: `(600, 32)`
- **pygame.Rect (get_rect)**: `(600, 32)`
- **rect.center**: `(400, 420)`
- **Blit Position (topleft)**: `(100, 404)`

### Registro 1 (rz: 33.5°)
- **Transform.position**: `(0.000000, 0.000000)`
- **screen_x / screen_y**: `(400.0000, 300.0000)`
- **Surface (antes rotação)**: `(36, 48)`
- **Surface (após rotação)**: `(56, 59)`
- **pygame.Rect (get_rect)**: `(56, 59)`
- **rect.center**: `(400, 300)`
- **Blit Position (topleft)**: `(372, 271)`

### Registro 2 (rz: 0.0°)
- **Transform.position**: `(0.000000, 120.000000)`
- **screen_x / screen_y**: `(400.0000, 420.0000)`
- **Surface (antes rotação)**: `(600, 32)`
- **Surface (após rotação)**: `(600, 32)`
- **pygame.Rect (get_rect)**: `(600, 32)`
- **rect.center**: `(400, 420)`
- **Blit Position (topleft)**: `(100, 404)`

### Registro 3 (rz: 33.5°)
- **Transform.position**: `(1.500000, 1.500000)`
- **screen_x / screen_y**: `(401.5000, 301.5000)`
- **Surface (antes rotação)**: `(36, 48)`
- **Surface (após rotação)**: `(56, 59)`
- **pygame.Rect (get_rect)**: `(56, 59)`
- **rect.center**: `(401, 301)`
- **Blit Position (topleft)**: `(373, 272)`

### Registro 4 (rz: 0.0°)
- **Transform.position**: `(0.000000, 120.000000)`
- **screen_x / screen_y**: `(400.0000, 420.0000)`
- **Surface (antes rotação)**: `(600, 32)`
- **Surface (após rotação)**: `(600, 32)`
- **pygame.Rect (get_rect)**: `(600, 32)`
- **rect.center**: `(400, 420)`
- **Blit Position (topleft)**: `(100, 404)`

### Registro 5 (rz: 33.5°)
- **Transform.position**: `(3.000000, 3.000000)`
- **screen_x / screen_y**: `(403.0000, 303.0000)`
- **Surface (antes rotação)**: `(36, 48)`
- **Surface (após rotação)**: `(56, 59)`
- **pygame.Rect (get_rect)**: `(56, 59)`
- **rect.center**: `(403, 303)`
- **Blit Position (topleft)**: `(375, 274)`

### Registro 6 (rz: 0.0°)
- **Transform.position**: `(0.000000, 120.000000)`
- **screen_x / screen_y**: `(400.0000, 420.0000)`
- **Surface (antes rotação)**: `(600, 32)`
- **Surface (após rotação)**: `(600, 32)`
- **pygame.Rect (get_rect)**: `(600, 32)`
- **rect.center**: `(400, 420)`
- **Blit Position (topleft)**: `(100, 404)`

### Registro 7 (rz: 33.5°)
- **Transform.position**: `(4.500000, 4.500000)`
- **screen_x / screen_y**: `(404.5000, 304.5000)`
- **Surface (antes rotação)**: `(36, 48)`
- **Surface (após rotação)**: `(56, 59)`
- **pygame.Rect (get_rect)**: `(56, 59)`
- **rect.center**: `(404, 304)`
- **Blit Position (topleft)**: `(376, 275)`

### Registro 8 (rz: 0.0°)
- **Transform.position**: `(0.000000, 120.000000)`
- **screen_x / screen_y**: `(400.0000, 420.0000)`
- **Surface (antes rotação)**: `(600, 32)`
- **Surface (após rotação)**: `(600, 32)`
- **pygame.Rect (get_rect)**: `(600, 32)`
- **rect.center**: `(400, 420)`
- **Blit Position (topleft)**: `(100, 404)`

### Registro 9 (rz: 33.5°)
- **Transform.position**: `(6.000000, 6.000000)`
- **screen_x / screen_y**: `(406.0000, 306.0000)`
- **Surface (antes rotação)**: `(36, 48)`
- **Surface (após rotação)**: `(56, 59)`
- **pygame.Rect (get_rect)**: `(56, 59)`
- **rect.center**: `(406, 306)`
- **Blit Position (topleft)**: `(378, 277)`

### Registro 10 (rz: 0.0°)
- **Transform.position**: `(0.000000, 120.000000)`
- **screen_x / screen_y**: `(400.0000, 420.0000)`
- **Surface (antes rotação)**: `(600, 32)`
- **Surface (após rotação)**: `(600, 32)`
- **pygame.Rect (get_rect)**: `(600, 32)`
- **rect.center**: `(400, 420)`
- **Blit Position (topleft)**: `(100, 404)`

### Registro 11 (rz: 160.7°)
- **Transform.position**: `(6.000000, 6.000000)`
- **screen_x / screen_y**: `(406.0000, 306.0000)`
- **Surface (antes rotação)**: `(36, 48)`
- **Surface (após rotação)**: `(49, 57)`
- **pygame.Rect (get_rect)**: `(49, 57)`
- **rect.center**: `(406, 306)`
- **Blit Position (topleft)**: `(382, 278)`

### Registro 12 (rz: 0.0°)
- **Transform.position**: `(0.000000, 120.000000)`
- **screen_x / screen_y**: `(400.0000, 420.0000)`
- **Surface (antes rotação)**: `(600, 32)`
- **Surface (após rotação)**: `(600, 32)`
- **pygame.Rect (get_rect)**: `(600, 32)`
- **rect.center**: `(400, 420)`
- **Blit Position (topleft)**: `(100, 404)`

### Registro 13 (rz: 177.8°)
- **Transform.position**: `(6.000000, 6.000000)`
- **screen_x / screen_y**: `(406.0000, 306.0000)`
- **Surface (antes rotação)**: `(36, 48)`
- **Surface (após rotação)**: `(37, 49)`
- **pygame.Rect (get_rect)**: `(37, 49)`
- **rect.center**: `(406, 306)`
- **Blit Position (topleft)**: `(388, 282)`

### Registro 14 (rz: 0.0°)
- **Transform.position**: `(0.000000, 120.000000)`
- **screen_x / screen_y**: `(400.0000, 420.0000)`
- **Surface (antes rotação)**: `(600, 32)`
- **Surface (após rotação)**: `(600, 32)`
- **pygame.Rect (get_rect)**: `(600, 32)`
- **rect.center**: `(400, 420)`
- **Blit Position (topleft)**: `(100, 404)`

### Registro 15 (rz: 195.5°)
- **Transform.position**: `(6.000000, 6.000000)`
- **screen_x / screen_y**: `(406.0000, 306.0000)`
- **Surface (antes rotação)**: `(36, 48)`
- **Surface (após rotação)**: `(47, 55)`
- **pygame.Rect (get_rect)**: `(47, 55)`
- **rect.center**: `(406, 306)`
- **Blit Position (topleft)**: `(383, 279)`

### Registro 16 (rz: 0.0°)
- **Transform.position**: `(0.000000, 120.000000)`
- **screen_x / screen_y**: `(400.0000, 420.0000)`
- **Surface (antes rotação)**: `(600, 32)`
- **Surface (após rotação)**: `(600, 32)`
- **pygame.Rect (get_rect)**: `(600, 32)`
- **rect.center**: `(400, 420)`
- **Blit Position (topleft)**: `(100, 404)`

### Registro 17 (rz: 213.5°)
- **Transform.position**: `(6.000000, 6.000000)`
- **screen_x / screen_y**: `(406.0000, 306.0000)`
- **Surface (antes rotação)**: `(36, 48)`
- **Surface (após rotação)**: `(56, 59)`
- **pygame.Rect (get_rect)**: `(56, 59)`
- **rect.center**: `(406, 306)`
- **Blit Position (topleft)**: `(378, 277)`

### Registro 18 (rz: 0.0°)
- **Transform.position**: `(0.000000, 120.000000)`
- **screen_x / screen_y**: `(400.0000, 420.0000)`
- **Surface (antes rotação)**: `(600, 32)`
- **Surface (após rotação)**: `(600, 32)`
- **pygame.Rect (get_rect)**: `(600, 32)`
- **rect.center**: `(400, 420)`
- **Blit Position (topleft)**: `(100, 404)`

### Registro 19 (rz: 231.5°)
- **Transform.position**: `(6.000000, 6.000000)`
- **screen_x / screen_y**: `(406.0000, 306.0000)`
- **Surface (antes rotação)**: `(36, 48)`
- **Surface (após rotação)**: `(59, 58)`
- **pygame.Rect (get_rect)**: `(59, 58)`
- **rect.center**: `(406, 306)`
- **Blit Position (topleft)**: `(377, 277)`

## Respostas aos Questionamentos da Auditoria

### 1. A largura da Surface muda durante o drag?

- **Com rotação estática:** **NÃO.** Se o objeto apenas translada sem girar, a largura da superfície e a largura pós-rotação permanecem perfeitamente constantes.

- **Durante a rotação ativa:** **SIM.** A largura da superfície pós-rotação muda constantemente devido ao recalculo da caixa delimitadora do Pygame (`pygame.transform.rotate`).

### 2. A altura muda durante o drag?

- **Com rotação estática:** **NÃO.**

- **Durante a rotação ativa:** **SIM.**

### 3. get_rect() retorna dimensões diferentes entre frames?

- **Com rotação estática:** **NÃO.**

- **Durante a rotação ativa:** **SIM.** As dimensões de `get_rect()` acompanham a mudança da caixa delimitadora da imagem girada.

### 4. rect.center sofre deslocamento mesmo quando screen_x permanece contínuo?

**SIM.** O `rect.center` é definido como `(int(screen_x), int(screen_y))`. Como há um truncamento para inteiros, o centro lógico sofre pequenos saltos discretos mesmo que a coordenada flutuante `screen_x` varie de forma perfeitamente linear e contínua.

### 5. Existe alguma compensação automática feita pelo pygame ao centralizar a imagem?

**NÃO.** O Pygame não realiza sub-pixel interpolation ou anti-aliasing na blit. Ele simplesmente define o centro geométrico do `Rect` como um ponto de grade inteiro. Se as dimensões da superfície mudarem de par para ímpar (ex: largura `49` para `50`), o pivô do centro geométrico é deslocado por meio pixel inteiro para compensar, gerando saltos de 1 pixel.

### 6. O pivô visual muda durante a rotação?

**SIM.** A rotação no Pygame aumenta a caixa delimitadora (bounding box). Se o tamanho do sprite rotacionado passar de par para ímpar ou vice-versa, o centro do `Rect` sofre micro-desvios de pixel de blit (truncamento do pivô), fazendo com que o objeto pareça 'mancar' ou se deslocar levemente do seu centro físico.

### 7. O rect é reconstruído usando bounding box dinâmica?

**SIM.** A chamada `rotated_img.get_rect()` reconstrói a caixa delimitadora do zero em cada chamada com base nas dimensões atuais da superfície rotacionada.

### 8. Existe jitter causado pela mudança do bounding box do pygame.transform.rotate()?

**SIM.** A mudança do bounding box ao girar (principalmente a transição de dimensões pares para ímpares) altera a matemática do cálculo do centro do `pygame.Rect`, induzindo tremulações adicionais (jitter) além do stuter de translação comum.