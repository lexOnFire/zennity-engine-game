# Dependencias de desenvolvimento (nao necessarias para rodar o jogo)
# Instale com: pip install -r requirements-dev.txt

# Testes
pytest>=7.4
pytest-cov>=4.1
pytest-env>=1.1          # suporte a [pytest] env = ... no pytest.ini
pytest-timeout>=2.4      # rede de seguranca de CI, NAO substitui teste deterministico
build>=1.2
tomli>=2.0; python_version < "3.11"

# Lint e formatacao
ruff>=0.4

# Type checking (opcional)
mypy>=1.8
